
{{ form_start(form) }}
<div class="sm:container mx-auto h-full">
  <div class="flex mx-auto">
    <div class="bg-gray-50/50 backdrop-blur-sm border border-gray-100 shadow-lg shadow-purple-300/40 w-2/5 p-5 rounded-lg h-auto mx-auto mt-[2rem]">
      <div class="flex flex-wrap -mx-3 md:mx-auto">

        <div class="w-full px-3 md:mb-0 mb-2 mt-4">
          <label class="block tracking-wide mb-2 text-gray-700">Nom de l'objet</label>
          	{{ form_widget(form.name, {'attr': {'class': 'appearance-none block w-full border-indigo-300 rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white', 'placeholder' : 'Nom de l\'objet'}})}}
        </div>
      </div>
      <div class="flex flex-wrap -mx-3 md:mx-auto mb-2">
        <div class="w-full px-3">
          <label class="block tracking-wide mb-2 text-gray-700">Description</label>
            {{ form_widget(form.description, {'attr': {'class': 'appearance-none block w-full border-indigo-300 rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500', 'placeholder' : 'Description'}})}}
        </div>
      </div>
       <div class="flex flex-wrap -mx-3 md:mx-auto mb-2">
        <div class="px-3 md:mb-0 my-2 w-full">
          <label class=" inline-block whitespace-nowrap tracking-wide mb-2 text-gray-700">Photo de l'objet</label>
            {{ form_widget(form.imageFile, {'attr': {'class': 'shadow-inner shadow-indigo-300/50 block rounded py-3 px-4 leading-tight focus:outline-none focus:border-gray-500 w-full'}} )}}
        </div>
      </div>
      <div class="flex flex-wrap -mx-3 md:mx-auto mb-2">
        <div class="w-full px-3 mb-6 md:mb-0">
          <label class="block tracking-wide mb-2 text-gray-700">Catégorie</label>
          <div class="relative">
            <div>
                   {{ form_widget(form.category)}}
            </div>
          </div>
        </div>
      </div>
      <div class="space-x-2 m-4 flex justify-center">
        <button type="submit" class="inline-block px-6 py-2.5 bg-gradient-to-r from-indigo-600 to-violet-600 text-white font-medium text-xs uppercase rounded shadow-md shadow-violet-300 hover:shadow-lg hover:shadow-violet-300">{{ button_label }}</button>
      </div>
    </div>
  </div>
</div>
{{ form_rest(form) }}
{{ form_end(form) }}
