<form method="post" action="{{ path('app_object_files_delete', {'id': object_file.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ object_file.id) }}">
    <button class="btn">Delete</button>
</form>
